provider:
  name: aws

env:
  - name: AWS_DEFAULT_REGION
    value: eu-west-1

sources:
  - ingress
  - service

domainFilters:
  - abdirahman.forum

extraArgs:
  - --zone-id-filter=Z10354822X25ZQ06MIGTG

txtOwnerId: external-dns
policy: sync

serviceAccount:
  create: true
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::292681319569:role/externaldns
